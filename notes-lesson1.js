//Lecture 1 JS Syntax-only



document.querySelector('');

var = ;
const = ;
let = ;

eventTarget.addEventListener(' ', );     //Not sure if quotations are required for first argument addEventListener(argument1,____);

function createAFunction() {
	
};

changeText.textContent = 'Change the text of the selected element';

alert('Create an alert, a pop-up text message');

<p onclick=nameInquiry()>

//Lecture 1 JS Code


//Step Two
document.querySelector('em');
let emVariable = document.querySelector('em');

//Step Three

emVariable.addEventListener('click', alert); // || emVariable.addEventListener('click', nameInquiry);

//Step Four
function alert(){
//let emVariable = document.querySelector('em');
alert('Questioning the validity of the duplicate function declaration');	
}

//Step 4a/4b
function nameInquiry(){
let name = prompt("First name please");	
emVariable.textContent = name;
}


//Step Five <em onclick=alert()>

//Step Six [HTML Link --> JS]

<script src="main.js">This is the land of JS, where are you Doctype?</script>

const javaOuch = document.querySelector('h1');
javaOuch.textContent = 'JavaScript Ouch...';

eventTarget.addEventListener('click',anAlert);


Lab 1 JS Syntax-only

console.log();

Math.floor();
Math.random();

document.querySelector('');

function ifElseFunction() {
	if(------) {
		}
	else if() {
	}
	else() {
	}
	
	
}
variable.value 
guess.value
_____.value

emVariable.textContent
changeText.textContent
_____.textContent


.addEventListener('click', argument2);






Lab 1 JS Code

var randomNumber = Math.floor(Math.random() * 100);

var querySelectExample = document.querySelector('#sloppySyntax);

function decideIfWon() {
	
 if (guess.value < randomNumber) {
    lowOrHi.textContent = "Too low";
 } else if (guess.value > randomNumber) {
    lowOrHi.textContent = "Too high";
 } else {
    lowOrHi.textContent = "One in 100 odds! Correct number!!"
  }
}

useless4nowButton.addEventListener('click', decideIfWon);







